# 2026-03-22 Handoff

## Current Status
- Middle-1 shared unit flow is implemented across overview, quiz, tutor, progress, and report.
- All 8 middle-1 units are registered and reachable in the app.
- Local PDF references in `C:\MathFile` are staged into plaintext fixtures under `/app/harness/copyright/fixtures/`.
- `content harness`, `tutor harness`, `extract-references`, `copyright harness`, `lint`, `build`, and `preflight` pass in the real shell environment.

## What Was Done Today
- Added workflow docs for large-batch safe problem authoring:
  - `/docs/workflows/problem-generation-workflow.md`
  - `/docs/workflows/primeFactorization-generation-template.md`
- Added a 10-problem batch to `/lib/content/primeFactorization.ts`.
- Expanded `primeFactorization` diagnostic quiz coverage.
- Added a 10-problem batch to `/lib/content/integersRational.ts`.
- Verified the new content against:
  - `harness:content`
  - `harness:tutor`
  - `harness:copyright`

## Latest Verification
Run in `/app` on 2026-03-22:

```powershell
npm.cmd run harness:content
npm.cmd run harness:tutor
$env:SUJI_REFERENCE_ROOT = Join-Path (Get-Location) 'harness/copyright/fixtures'
npm.cmd run harness:copyright
npm.cmd run lint
npm.cmd run build
npm.cmd run harness:preflight
```

Results:
- `harness:content`: pass with `errors=0, warnings=0`
- `harness:tutor`: pass with `errors=0, warnings=0`
- `harness:copyright`: pass with `safe=8, review=0, high-risk=0`
- `lint`: pass
- `build`: pass
- `preflight`: pass with `status=ready`

## Known Limits
- PDF extraction still leaves some symbol and number noise because a few reference PDFs use custom embedded fonts.
- The new batch-writing workflow is only applied to `primeFactorization` and `integersRational` so far.
- `C:\MathFile` remains local reference input only and is not part of git or deployment.

## Recommended Next Task
- Apply the same batch expansion workflow to `literalExpressions`, then continue unit-by-unit.

## Suggested Order
1. Use the workflow doc to generate a `literalExpressions` batch.
2. Run `harness:content` and fixture-based `harness:copyright`.
3. If clean, expand quiz and tutor-facing coverage for that unit too.
4. Continue the same pattern for `coordinatePlane`.

## Files To Read First Next Time
1. `PROJECT_CONTEXT.md`
2. `NEXT_STEPS.md`
3. `docs/handoffs/2026-03-22-handoff.md`

## Resume Prompt
- Continue from `docs/handoffs/2026-03-22-handoff.md`. Next apply the batch-generation workflow to `literalExpressions`, then validate and keep expanding the remaining units.
