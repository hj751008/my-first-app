# 2026-03-21 Handoff

## Current Status
- Middle-1 shared unit flow is implemented across overview, quiz, tutor, progress, and report.
- All 8 middle-1 units are registered and reachable in the app.
- Local PDF references in `C:\MathFile` are now staged into plaintext fixtures under `/app/harness/copyright/fixtures/`.
- `content harness`, `tutor harness`, `extract-references`, `copyright harness`, `lint`, `build`, and `preflight` all pass in the real shell environment.

## What Was Done
- Converted the reference workflow from HWP/PDF-only scanning to fixture-based plaintext comparison.
- Installed `pypdf` in the user Python environment and added `/app/harness/reference-extract/extract_pdf_references.py`.
- Added `npm run harness:extract-references` in `/app/package.json`.
- Added `/app/harness/copyright/fixtures/README.md` and `/app/harness/copyright/fixtures/reference-manifest.json`.
- Updated the manifest from old `.hwp` paths to current `.pdf` paths in `C:\MathFile`.
- Tuned PDF extraction normalization to:
  - remove private-use glyph noise
  - drop low-signal corrupted lines
  - trim common answer-section pages by source-family page limits
- Updated `/app/harness/copyright/similarity-check.mts` to:
  - use runtime `SUJI_REFERENCE_ROOT`
  - avoid import-time side effects
  - normalize `NFKC` and private-use characters before comparison
- Added `copyright harness` to `/app/harness/preflight/run-preflight.mts` using the local fixture directory.
- Added `"type": "module"` to `/app/package.json` so the previous Node module warning no longer appears.
- Updated repo docs:
  - `/PROJECT_CONTEXT.md`
  - `/NEXT_STEPS.md`

## Latest Verification
Run in `/app` on 2026-03-21:

```powershell
$env:PYTHONIOENCODING = 'utf-8'
npm.cmd run harness:extract-references
$env:SUJI_REFERENCE_ROOT = Join-Path (Get-Location) 'harness/copyright/fixtures'
npm.cmd run harness:copyright
npm.cmd run lint
npm.cmd run build
npm.cmd run harness:preflight
```

Results:
- `harness:extract-references`: pass with `groups=8, sources=27, extracted=27`
- `harness:copyright`: pass with `safe=8, review=0, high-risk=0`
- `lint`: pass
- `build`: pass
- `preflight`: pass with `status=ready`

## Known Limits
- PDF extraction still leaves some symbol and number noise because a few reference PDFs use custom embedded fonts.
- Fixture quality is much better after page trimming, but it is still heuristic and should be reviewed if the source set changes.
- `C:\MathFile` is a local reference store only and is not part of git or deployment.
- Inside the Codex sandbox, `preflight` can still show `ready-with-manual-check` because child process spawning may be blocked even when the real shell result is `ready`.

## Recommended Next Task
- Review the generated fixture files and decide whether they should be committed as stable repo inputs or regenerated locally when needed.

## Suggested Order
1. Spot-check `/app/harness/copyright/fixtures/*.sources.txt` for remaining low-signal extraction artifacts.
2. Commit the generated fixture files as stable repo inputs unless the reference workflow changes.
3. If fixture quality is acceptable, return to expanding content packs and tutor scenarios.
4. After that, start planning middle-2 and middle-3 shared structure.

## Files To Read First Next Time
1. `PROJECT_CONTEXT.md`
2. `NEXT_STEPS.md`
3. `docs/handoffs/2026-03-21-handoff.md`

## Resume Prompt
- Continue from `docs/handoffs/2026-03-21-handoff.md`. First review the committed copyright fixtures, then return to content expansion.
